export const sttService = {
  async transcribe(audioUri: string, language: string = 'en-IN'): Promise<string> {
    // TODO (AKASH): Implement Bhashini STT in Phase 5
    return 'Can I go fishing tomorrow morning near Kakinada?';
  },
};
